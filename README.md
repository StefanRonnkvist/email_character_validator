# email_character_validator

This project accepts an eMail as a string and returns a RichText. The RichText colors each character as valid, invalid or warning as to the email name passing through servers for delivery. The code provides the methods to combine Widgets and discrete code to provide functioning application of theory of Dart and Flutter.